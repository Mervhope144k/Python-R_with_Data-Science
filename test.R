# Author : Merveilles Katumba Biende
# This program takes a single number and calculate a 
# value of 10 times of it
a = 14 # the input number
b = a*10 #b is calculated as 10 times "a"
# "=" and "<-" both can be used as the assignment operators
# the next statement prints the output
print(c(a,b))